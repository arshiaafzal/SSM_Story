<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://arshiaafzal.github.io/SSM_Story/feed.xml" rel="self" type="application/atom+xml"/><link href="https://arshiaafzal.github.io/SSM_Story/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-07T18:49:18+00:00</updated><id>https://arshiaafzal.github.io/SSM_Story/feed.xml</id><title type="html">blank</title><subtitle></subtitle><entry><title type="html">Phoenix (Part-1)</title><link href="https://arshiaafzal.github.io/SSM_Story/2025/lion-part1-model_old/" rel="alternate" type="text/html" title="Phoenix (Part-1)"/><published>2025-08-29T00:00:00+00:00</published><updated>2025-08-29T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/SSM_Story/2025/lion-part1-model_old</id><content type="html" xml:base="https://arshiaafzal.github.io/SSM_Story/2025/lion-part1-model_old/"><![CDATA[<h2 id="the-recall-gap">The Recall Gap</h2> <p>Mamba <d-cite key="mamba"></d-cite> showed that attention <d-cite key="attention"></d-cite> is not strictly necessary for strong sequence modeling. A simple recurrent model could match, and sometimes outperform, Transformers on language modeling at a fraction of the cost. But there is no free lunch. Soon after, several works exposed a critical limitation: fixed-size memory models struggle on recall benchmarks, which is not surprising, since they have only a finite amount of room to store information. While it’s tempting to blame their finite capacity, the reality is more nuanced.</p> <p>Consider Needle-In-The-Haystack (NIAH). The model is first given a lookup key, then a long dictionary, and finally asked to retrieve the matching value. This is a form of <strong>constant-memory recall</strong>: in principle, it requires only $O(1)$ memory, since the model only needs to retain the key and its corresponding value as it scans the sequence (unlike <strong>scaling-memory recall</strong> tasks like KV-Retrieval, which require $O(T)$ memory because the query comes after the dictionary).</p> <p>Despite this theoretical $O(1)$ requirement, recurrent models still lag behind Transformers on NIAH. This failure isn’t just a quirk of synthetic benchmarks; it mirrors real-world bottlenecks. In multiple-choice questions, a linear model may identify the correct answer early on but fail to preserve that single letter in its state until the output <d-cite key="gna"></d-cite>. Similarly, in math and code, models struggle to track a single variable or a few intermediate values over long distances.</p> <p>The issue, then, isn’t a lack of physical room—it’s a lack of <strong>persistence</strong>: the ability to leave specific information untouched. The model has the capacity; it just lacks the organization.</p> <h2 id="organize-your-memory-like-your-closet">Organize your memory like your closet 👕</h2> <blockquote> <p><em>Can we design a recurrent model with strong constant-memory recall that approach their theoretical limits?</em></p> </blockquote> <p>To understand why persistence is important, think of your hidden state as a closet. When you buy a new piece of clothing, you don’t shove it into a giant pile on the floor, nor do you throw out your favorite jacket just to fit a new shirt. Instead, you take two deliberate steps:</p> <p><strong>1. <em>Decide which shelf it belongs on.</em></strong> (Routing) <br/> <strong>2. <em>Reorganize that shelf to make room.</em></strong> (Squeezing / Decay)</p> <p>In this analogy, the <strong>closet</strong> is the memory, the <strong>shelves</strong> are memory slots, and tokens are the <strong>clothes</strong>. Current models fail because they only perform one of these steps effectively.</p> <h3 id="ssms-closet-is-a-pile-of-clothes">SSMs’ closet is a pile of clothes</h3> <p>State space models (SSMs) <d-cite key="mamba,mamba2"></d-cite> and linear attention <d-cite key="linearattn"></d-cite> all share the same fundamental memory operation — a matrix-valued hidden state $S_t \in \mathbb{R}^{M\times d}$ updated by a <strong>linear time-dependent</strong> recurrence:</p> \[S_t = \underbrace{S_{t-1}A_t}_{\text{Decay}} + \underbrace{v_t k_t^\top}_{\text{Write}}, \qquad o_t = \underbrace{S_t q_t}_{\text{Read}}\] <p>SSMs are masters of the <strong>reorganizing</strong> step. The forget gate $A_t$ acts as a mechanism for decaying old content to make room for the new. However, they lack the ability to choose a shelf.</p> <p>In the diagonal case ($A_t=\text{diag}(a_t)$), the update simplifies to $S_t = S_{t-1} \odot a_t + v_t k_t^\top$. Here, the scalar $a_t$ squeezes information uniformly across the entire state—like compressing every shelf in the closet simultaneously. Because the write $v_t k_t^\top$ lands across all $M$ rows, every new token dilutes everything previously stored. There is no isolation; there is only the pile.</p> <div style="margin: 1.5rem auto 1rem; display: flex; justify-content: center; width: 100%;"> <iframe id="ssm-recurrent-matrix-update" src="/SSM_Story/assets/html/ssm_recurrent_matrix_update.html" title="SSM recurrent matrix update visualization" loading="lazy" scrolling="no" style="display: block; width: 70%; max-width: 1400px; height: 0; border: 0; border-radius: 0rem; background: transparent; overflow: hidden;"></iframe> </div> <h3 id="swas-closet-throws-out-the-old-clothes">SWA’s closet throws out the old clothes</h3> <p>Sliding window attention (SWA) takes the opposite approach. It maintains a fixed cache and, at each step, $\color{red}{\text{drops}}$ the oldest token to $\color{green}{\text{append}}$ the newest one. Written as a matrix update using a one-hot selector $e_t$:</p> \[[S^{k}_t, S^v_t] = \underbrace{(\mathbf{1} - e_t)}_{\color{red}{\text{Remove oldest}}}\odot[S^{k}_{t-1}, S^v_{t-1}] + \underbrace{e_t}_{\color{green}{\text{Write new}}} [k_t^\top, v_t^\top]\] <p>SWA is great at choosing a shelf; it writes to exactly one slot ($e_t$). But it cannot reorganize. It doesn’t squeeze old content to make room; it deletes it ruthlessly to make space for the new.</p> <div style="margin: 1.5rem auto 1rem; display: flex; justify-content: center; width: 100%;"> <iframe id="swa-recurrent-matrix-update" src="/SSM_Story/assets/html/swa_recurrent_matrix_update.html" title="SWA recurrent matrix update visualization" loading="lazy" scrolling="no" style="display: block; width: 70%; max-width: 1400px; height: 0; border: 0; border-radius: 0rem; background: transparent; overflow: hidden;"></iframe> </div> <h3 id="the-missing-piece">The Missing Piece</h3> <p>If SSMs write blindly and SWA deletes ruthlessly, the solution is a model that can choose where to write based on what it is seeing.</p> <p>Bridging the recall gap requires a single mechanism that can do both: treat subsets of the hidden state as independent slots that can be selectively updated or gracefully preserved. This dual requirement—routing and decay—is the foundation of the framework.</p> <h2 id="routing-slot-memories">Routing Slot Memories</h2> <p>Instead of SWA’s fixed ring-buffer ($e_t$), what if we used a learned, sparse router vector $r_t \in \mathbb{R}^M$? This router looks at the current token and picks exactly which “shelves” it belongs on. This is the core of Routing Slot Memories (RSMs):</p> \[S_t = \underbrace{(\mathbf{1} - r_t) \odot S_{t-1}}_{\text{Preserved Shelves}} + \underbrace{r_t \odot \bigl(D_t\,S_{t-1}\,A_t + U_t\bigr)}_{\text{Updated Shelves}}\] <p>The beauty of this formulation is that it’s a spectrum. Because $r_t$ is sparse, most entries are zero—meaning most of the memory is left completely untouched.</p> <ul> <li>The First Term $(\mathbf{1} - r_t) \odot S_{t-1}$ is the persistence mechanism. It ensures that any shelf not selected by the router remains perfectly preserved.</li> <li>The Second Term $r_t \odot (\dots)$ is the selective update. Only the chosen shelves undergo the “squeezing” ($D_t, A_t$) and “writing” ($U_t$) process.</li> </ul> <p>By simply changing the behavior of the router $r_t$, this single equation recovers the entire landscape of current sequence models:x</p> <div style="margin: 1.5rem auto 1rem; display: flex; justify-content: center; width: 100%; overflow-x: auto;"> <iframe id="comparison-table" src="/SSM_Story/assets/html/comparison_table.html" title="Comparison table" loading="lazy" scrolling="no" style="display: block; width: 100%; max-width: 100%; height: 0; border: 0; border-radius: 0rem; background: transparent; overflow: hidden;"></iframe> </div> <p>A dense router ($r_t = \mathbf{1}$) gives you an SSM—every token hits every shelf. A one-hot cyclic router ($r_t = e_t$) gives you SWA—every token hits the oldest shelf.</p> <p>The question then became concrete: Can we train a model to learn, purely from data, which shelf each token belongs on—and keep it there?</p> <p>Our answer is <strong>Phoenix</strong>. By using a <strong>learned sparse router</strong>, Phoenix treats its hidden state like a Mixture-of-Experts <d-cite key="moe"></d-cite> for memory. Much like an organized closet, it can tuck a needle into a dedicated “retrieval shelf” and leave it undisturbed by the thousands of “filler” tokens flowing through other slots.</p> <p>In <strong>Part 2</strong>, we’ll move from theory to practice: we’ll look at the specific architecture of the Phoenix block, the “counterintuitive” design decisions that made it work, and how this organized memory allows it to recall information $16\times$ beyond its training length.</p> <script>
  (() => {
    const iframe = document.getElementById('swa-recurrent-matrix-update');
    if (!iframe) return;

    const resizeIframe = () => {
      try {
        const doc = iframe.contentDocument || iframe.contentWindow?.document;
        if (!doc || !doc.body || !doc.documentElement) return;

        const height = Math.max(
          doc.body.scrollHeight,
          doc.documentElement.scrollHeight,
          doc.body.offsetHeight,
          doc.documentElement.offsetHeight
        );

        iframe.style.height = `${height}px`;
      } catch (error) {
        // Ignore cross-document timing issues during initial load.
      }
    };

    iframe.addEventListener('load', () => {
      resizeIframe();
      window.setTimeout(resizeIframe, 0);
      window.setTimeout(resizeIframe, 250);
      window.setTimeout(resizeIframe, 1000);
    });

    window.addEventListener('resize', resizeIframe);
  })();
</script> <script>
  (() => {
    const iframe = document.getElementById('ssm-recurrent-matrix-update');
    if (!iframe) return;

    const resizeIframe = () => {
      try {
        const doc = iframe.contentDocument || iframe.contentWindow?.document;
        if (!doc || !doc.body || !doc.documentElement) return;

        const height = Math.max(
          doc.body.scrollHeight,
          doc.documentElement.scrollHeight,
          doc.body.offsetHeight,
          doc.documentElement.offsetHeight
        );

        iframe.style.height = `${height}px`;
      } catch (error) {
        // Ignore cross-document timing issues during initial load.
      }
    };

    iframe.addEventListener('load', () => {
      resizeIframe();
      window.setTimeout(resizeIframe, 0);
      window.setTimeout(resizeIframe, 250);
      window.setTimeout(resizeIframe, 1000);
    });

    window.addEventListener('resize', resizeIframe);
  })();
</script> <script>
  (() => {
    const iframe = document.getElementById('comparison-table');
    if (!iframe) return;

    const resizeIframe = () => {
      try {
        const doc = iframe.contentDocument || iframe.contentWindow?.document;
        if (!doc || !doc.body || !doc.documentElement) return;

        const height = Math.max(
          doc.body.scrollHeight,
          doc.documentElement.scrollHeight,
          doc.body.offsetHeight,
          doc.documentElement.offsetHeight
        );

        iframe.style.height = `${height}px`;
      } catch (error) {
        // Ignore cross-document timing issues during initial load.
      }
    };

    iframe.addEventListener('load', () => {
      resizeIframe();
      window.setTimeout(resizeIframe, 0);
      window.setTimeout(resizeIframe, 250);
      window.setTimeout(resizeIframe, 1000);
    });

    window.addEventListener('resize', resizeIframe);
  })();
</script>]]></content><author><name>Arshia Afzal$^*$</name></author><summary type="html"><![CDATA[Memory as a set of Slots]]></summary></entry><entry><title type="html">Story of Linear Time Sequence Modeling 📚</title><link href="https://arshiaafzal.github.io/SSM_Story/2025/lion-part3-chunk/" rel="alternate" type="text/html" title="Story of Linear Time Sequence Modeling 📚"/><published>2025-08-29T00:00:00+00:00</published><updated>2025-08-29T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/SSM_Story/2025/lion-part3-chunk</id><content type="html" xml:base="https://arshiaafzal.github.io/SSM_Story/2025/lion-part3-chunk/"><![CDATA[<hr/> <h1 id="why-linear-transformers">Why Linear Transformers?</h1> <p>If you’ve heard about Large Language Models (LLMs) and Transformers and are curious to learn more, there are already plenty of excellent blog posts, articles, and YouTube videos that explain them in great detail with amazing visualizations. In case your main interest is understanding LLMs and how Transformers work, we’d point you there first, they’ve done a fantastic job (honestly, better than us).</p> <p>In this series, we’ll cover Linear Transformers and State Space Models (SSMs), giving a high-level summary of their core ideas. So, if you’ve come across names like <strong>Mamba</strong> or <strong>DeltaNet</strong> and wondered what they are, or if you’ve asked yourself:</p> <p><strong>1)</strong> Why we moved from RNNs to Transformers?</p> <p><strong>2)</strong> Why we now seem to be circling back from Transformers toward Linear Transformers (almost like “RNNs on steroids”)?</p> <p>then this post should be a good fit 😉.</p> <p>So let’s start from answering the above questions</p> <hr/>]]></content><author><name>Arshia</name></author><summary type="html"><![CDATA[Summary of Linear Transformers]]></summary></entry><entry><title type="html">Story of Linear Time Sequence Modeling 📚</title><link href="https://arshiaafzal.github.io/SSM_Story/2025/lion-part4-results/" rel="alternate" type="text/html" title="Story of Linear Time Sequence Modeling 📚"/><published>2025-08-29T00:00:00+00:00</published><updated>2025-08-29T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/SSM_Story/2025/lion-part4-results</id><content type="html" xml:base="https://arshiaafzal.github.io/SSM_Story/2025/lion-part4-results/"><![CDATA[<hr/> <h1 id="why-linear-transformers">Why Linear Transformers?</h1> <p>If you’ve heard about Large Language Models (LLMs) and Transformers and are curious to learn more, there are already plenty of excellent blog posts, articles, and YouTube videos that explain them in great detail with amazing visualizations. In case your main interest is understanding LLMs and how Transformers work, we’d point you there first, they’ve done a fantastic job (honestly, better than us).</p> <p>In this series, we’ll cover Linear Transformers and State Space Models (SSMs), giving a high-level summary of their core ideas. So, if you’ve come across names like <strong>Mamba</strong> or <strong>DeltaNet</strong> and wondered what they are, or if you’ve asked yourself:</p> <p><strong>1)</strong> Why we moved from RNNs to Transformers?</p> <p><strong>2)</strong> Why we now seem to be circling back from Transformers toward Linear Transformers (almost like “RNNs on steroids”)?</p> <p>then this post should be a good fit 😉.</p> <p>So let’s start from answering the above questions</p> <hr/>]]></content><author><name>Arshia</name></author><summary type="html"><![CDATA[Summary of Linear Transformers]]></summary></entry></feed>