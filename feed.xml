<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://arshiaafzal.github.io/SSM_Story/feed.xml" rel="self" type="application/atom+xml"/><link href="https://arshiaafzal.github.io/SSM_Story/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-03-27T19:00:21+00:00</updated><id>https://arshiaafzal.github.io/SSM_Story/feed.xml</id><title type="html">blank</title><subtitle></subtitle><entry><title type="html">Phoenix (Part-1)</title><link href="https://arshiaafzal.github.io/SSM_Story/2025/lion-part1-model/" rel="alternate" type="text/html" title="Phoenix (Part-1)"/><published>2025-08-29T00:00:00+00:00</published><updated>2025-08-29T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/SSM_Story/2025/lion-part1-model</id><content type="html" xml:base="https://arshiaafzal.github.io/SSM_Story/2025/lion-part1-model/"><![CDATA[<div style="width: 50%; margin: 0 auto;"> <figure> <picture> <source class="responsive-img-srcset" srcset="/SSM_Story/assets/img/best.PNG" sizes="95vw"/> <img src="/SSM_Story/assets/img/best.PNG" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <hr/> <h2 id="a-nagging-problem">A Nagging Problem</h2> <p>It started with a nagging feeling.</p> <p>Mamba <d-cite key="mamba"></d-cite> had just shown the world that you didn’t need attention <d-cite key="attention"></d-cite> to build powerful sequence models. A simple, elegant recurrence could match and even beat Transformers on language modeling. We were excited — the efficiency gains alone were compelling. But then we started running recall benchmarks.</p> <p>The results were humbling. Ask a linear model to retrieve a passkey buried deep in a long document, and it struggles. Ask it to remember a variable name from thousands of tokens ago in a codebase, and it forgets. Model after model — Mamba, Mamba-2 <d-cite key="mamba,mamba2"></d-cite> and their descendants — hit the same wall. The fixed-size memory that made them fast was also making them forgetful.</p> <blockquote> <p><em>Can we design SSMs with better recall even with fixed-size memory?</em></p> </blockquote> <p>That question sat with us for a while. We kept coming back to the same underlying puzzle: it’s not that the memory is too small — it’s that the model doesn’t know <em>how</em> to use it. Every new token gets smeared across the entire hidden state, diluting whatever was carefully stored before. The model isn’t choosing where to write; it’s writing everywhere at once.</p> <p>Then, one afternoon, someone made an offhand remark about organizing a closet — and everything clicked.</p> <p>Think about it. When you buy a new piece of clothing, you don’t shove it randomly onto every shelf simultaneously. You take two deliberate steps:</p> <p><strong>1. <em>Decide which shelf it belongs on.</em></strong> <strong>2. <em>Squeeze and reorganize that shelf to make room.</em></strong></p> <p>What if an SSM could do the same? <strong>The closet is the memory</strong>, <strong>the shelves are the memory slots</strong>, and <strong>tokens are the clothes</strong>. The squeezing part — making room by decaying old content — SSMs already do. What they don’t do is <em>choose</em> which shelf to write to. They treat the entire hidden state as one big shelf.</p> <p>That was the missing piece. We first needed to understand precisely what SSMs and their counterparts were doing to memory, and then figure out how to give them the ability to <em>choose</em>.</p> <h2 id="what-are-ssms-actually-doing">What Are SSMs Actually Doing?</h2> <p>To build something better, we had to look carefully under the hood.</p> <p>State space models <d-cite key="mamba,mamba2"></d-cite> and linear transformers <d-cite key="linearattn"></d-cite> all share the same fundamental memory operation — a matrix-valued hidden state $S_t \in \mathbb{R}^{M\times d}$ updated by a <strong>linear time-dependent</strong> recurrence:</p> \[S_t = \underbrace{S_{t-1}A_t}_{\text{Decay}} + \underbrace{v_t k_t^\top}_{\text{Write}}, \qquad o_t = \underbrace{S_t q_t}_{\text{Read}}\] <p>At each step $t$, the input $x_t \in \mathbb{R}^d$ is projected into queries, keys, and values: $q_t, k_t \in \mathbb{R}^d$ and $v_t \in \mathbb{R}^M$ <d-footnote> SSMs are often written with ($x,B,C$); we use $(v,k,q)$ for a read/write interpretation and to stay consistent with later attention-based special cases (e.g., sliding-window attention). </d-footnote>. The output $o_t\in \mathbb{R}^d$ is read from the state, and the forget gate $A_t \in \mathbb{R}^{d\times d}$ controls how much of the past survives.</p> <p>Different models instantiate $A_t$ differently, but they all use it as a mechanism for <em>squeezing</em> the past to make room for the present:</p> <details><summary>Forget Gates $A_t$ of Different Linear Models</summary> <ul> <li> <p><strong>Mamba-2</strong> <d-cite key="mamba2"></d-cite>: $A_t = a_t = \exp (-\mathrm{SoftPlus}(w^\top x_t)\,\exp(\Delta) )$</p> </li> <li> <p><strong>GLA</strong> <d-cite key="gla"></d-cite>: $A_t = \mathrm{diag}(\sigma(W^\top x_t)^\tau)$</p> </li> <li> <p><strong>DeltaNet</strong> <d-cite key="deltanet"></d-cite>: $A_t = I - \sigma(w^\top x_t) k_t k_t^\top$</p> </li> <li> <p><strong>Gated DeltaNet</strong> <d-cite key="gated_deltanet"></d-cite>: $A_t = a_t \left(I - \sigma(w^\top x_t) k_t k_t^\top\right)$</p> </li> </ul> </details> <p>In the diagonal case $A_t=\text{diag}(a_t)\in\mathbb{R}^d$, the update simplifies to:</p> \[S_t = S_{t-1} \odot a_t + v_t k_t^\top\] <p>The scalar $a_t$ squeezes information uniformly across all $d$ channels — like compressing every shelf in the closet at once. Even for models like DeltaNet and Gated DeltaNet where the non-diagonal $A_t$ also reshuffles content between channels, the write $v_t k_t^\top$ still lands across <em>all</em> $M$ rows of $S_t$. There is no choosing. Every token touches every shelf.</p> <p>This, we realized, is the root of the recall problem. And once we saw it clearly, we started wondering: is there another model out there that does something completely different?</p> <h2 id="the-other-extreme--swa">The Other Extreme — SWA</h2> <p>There is. And it had been hiding in plain sight.</p> <p>Sliding window attention keeps a fixed cache of the past $M$ tokens, and at every step it does something deceptively simple: it <strong>$\color{red}{\text{drops}}$</strong> the oldest token and <strong>$\color{green}{\text{appends}}$</strong> the newest one. With key and value caches $S^k_t, S^v_t\in \mathbb{R}^{M\times d}$:</p> \[[S^k_t, S^v_t] = [S^k_{t-1}, S^v_{t-1}].{\color{red}{\text{drop}}\color{black}{(k_{t-M}, v_{t-M})}}.{\color{green}{\text{append}}\color{black}{(k_t, v_t)}}, \qquad o_t= (S^v_t)^\top \text{SoftMax}(S^k_t q_t)\] <p>Written as a matrix update — using a one-hot selector $e_t$ that points to the oldest slot — this becomes:</p> \[[S^{k}_t, S^v_t] = \underbrace{(\mathbf{1} - e_t)}_{\color{red}{\text{Remove last token}}}\odot[S^{k}_{t-1}, S^v_{t-1}] + \underbrace{e_t}_{\color{green}{\text{Write new token}}} [k_t^\top, v_t^\top]\] <p>where the selector cycles through slots like a ring buffer:</p> \[e_t[i] = \begin{cases} 1 &amp; \text{if } i = m_t \\ 0 &amp; \text{otherwise} \end{cases} \quad \text{where} \quad m_t = 1 + ((t-1)\bmod M)\] <p>Here was the revelation: <strong>SWA chooses where to write</strong>. Unlike SSMs, which broadcast every token across all slots, SWA writes each token to exactly one slot — the one holding the oldest entry. Naive, yes — but it genuinely <em>is</em> choosing.</p> <p>The tradeoff, though, is brutal. SWA doesn’t squeeze the old content; it <strong>erases it entirely</strong> — like clearing a shelf completely before placing a new shirt on it. SSMs squeeze gracefully but write blindly. SWA writes precisely but deletes ruthlessly. Both approaches are incomplete on their own.</p> <p>We stared at this contrast for a while. Two models, each with exactly one of the two capabilities we needed. And that made us suspect that hybrid models — the ones combining SWA layers with SSM layers — derive much of their power precisely from this complementarity: each component doing something fundamentally different from the other.</p> <blockquote> <p>So we set out to design a single sequence mixer that could both <strong>choose</strong> where to write <em>and</em> <strong>gracefully decay</strong> what’s already there.</p> </blockquote> <p>And as a bonus — much like organizing a closet by clothing type — this turns out to naturally cluster similar tokens into similar memory slots.</p> <h2 id="routing-slot-memories">Routing Slot Memories</h2> <p>The key idea, in hindsight, is surprisingly clean.</p> <p>Instead of the ring-buffer’s fixed one-hot selector $e_t$, what if we used a learned, <em>sparse</em> router vector $r_t \in \mathbb{R}^M$ that picks which slots each token gets written to — based on the <em>content</em> of that token? This is the heart of <strong>Routing Slot Memories (RSMs)</strong>:</p> \[S_t = \underbrace{(\mathbf{1} - r_t) \odot S_{t-1}}_{\text{Preserved memory}} + \underbrace{r_t \odot \bigl(D_t\,S_{t-1}\,A_t + U_t\bigr)}_{\text{Updated memory}},\] <p>The router $r_t$ is sparse — most entries are zero. Only the selected slots get updated; the rest are left completely untouched. This is the <em>choosing</em> step. Within those selected slots, the term $D_t S_{t-1} A_t + U_t$ handles the <em>squeezing and writing</em> — and by varying $D_t$, $A_t$, and $U_t$, this single formulation recovers the full spectrum from SSMs to SWA as special cases.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/SSM_Story/assets/img/tab-480.webp 480w,/SSM_Story/assets/img/tab-800.webp 800w,/SSM_Story/assets/img/tab-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/SSM_Story/assets/img/tab.png" width="100%" height="auto" title="RSM Spectrom" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption"> Design of the Router and Decay components across multiple architectures. The bar on the right along with row colors display each model's spectrum as a function of router sparsity, with SWA and SSMs marking the two extremes.</figcaption> </figure> <p>Dense router $r_t = \mathbf{1}$ gives you an SSM. One-hot $r_t = e_t$ gives you SWA. Everything in between is new territory.</p> <p>For every token, only the content of the selected slots gets decayed — exactly like squeezing a specific shelf to make room, while leaving the other shelves untouched.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/SSM_Story/assets/img/mem-480.webp 480w,/SSM_Story/assets/img/mem-800.webp 800w,/SSM_Story/assets/img/mem-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/SSM_Story/assets/img/mem.png" width="100%" height="auto" title="RSM Vis" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption"> Visualization of three different sequence mixers using Routing Slot Memories, with different router choices. (a) SWA memory allocation uses a first-in-first-out strategy with a one-hot vector $\mathbf{e}_t$ as the router. (b) SSM memory allocation projects each token into all memory slots using a dense, all-ones router $\mathbf{1}_M$. (c) Phoenix memory allocation uses a selective router for writes. The visualization shows a sequence of $T=6$ tokens and $M=4$ memory slots for the hidden state $\mathbf{S}_t$, with $\text{Top}_K = 1$ for the \ours router.</figcaption> </figure> <p>And so the question became concrete:</p> <blockquote> <p>Can we train a model that learns, purely from data, <em>which shelf each token belongs on</em> — and then keeps it there?</p> </blockquote> <p>The answer is <strong>Phoenix</strong> — a model with a <strong>learned sparse router $r_t$</strong>, trained end-to-end, to route tokens to memory slots based on content. From a <strong>MoE</strong> <d-cite key="moe"></d-cite> perspective, the slots act as specialized experts: a slot that learns to hold passkeys keeps holding passkeys, undisturbed by the stream of ordinary text flowing through the other shelves.</p> <p>This is why Phoenix can store a passkey once and retrieve it 32,000 tokens later. Not because the memory is larger — but because it’s organized.</p> <p>In Part 2, we’ll walk through how we actually built Phoenix from this framework, the design decisions that surprised us along the way, and what happened when we finally ran the experiments.</p> <hr/> <div style="text-align: center;"> <img src="/SSM_Story/assets/video/best.gif" alt="animation" style="width: 60%; max-width: 600px;"/> </div>]]></content><author><name>Arshia Afzal$^*$ (Bloggin 🎙️)</name></author><summary type="html"><![CDATA[Memory as a set of Slots]]></summary></entry><entry><title type="html">Phoenix (Part-2)</title><link href="https://arshiaafzal.github.io/SSM_Story/2025/lion-part2-theory/" rel="alternate" type="text/html" title="Phoenix (Part-2)"/><published>2025-08-29T00:00:00+00:00</published><updated>2025-08-29T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/SSM_Story/2025/lion-part2-theory</id><content type="html" xml:base="https://arshiaafzal.github.io/SSM_Story/2025/lion-part2-theory/"><![CDATA[<div style="width: 50%; margin: 0 auto;"> <figure> <picture> <source class="responsive-img-srcset" srcset="/SSM_Story/assets/img/best.PNG" sizes="95vw"/> <img src="/SSM_Story/assets/img/best.PNG" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <hr/> <h2 id="from-framework-to-model">From Framework to Model</h2> <p>In Part 1, we arrived at Routing Slot Memories — a framework that could, in principle, combine the selective writing of SWA with the graceful forgetting of SSMs. Now came the harder question: how do you actually <em>build</em> this thing?</p> <p>The RSM framework is general by design. To get Phoenix, we had to make concrete choices about how the router $r_t$ works and what kind of decay $a_t$ to use. We wanted something expressive enough to learn meaningful slot assignments, but simple enough to train stably at scale.</p> <p>The recurrence we settled on uses two separate caches — one for keys, one for values — mirroring SWA’s structure, but with a learned decay instead of hard deletion:</p> \[S^k_t = (1-\exp(a_tr_t)) \odot S^k_{t-1} + \exp(a_tr_t)^\top k_t\] \[S^v_t = (1-\exp(a_tr_t)) \odot S^v_{t-1} + \exp(a_tr_t)^\top v_t\] <p>Unlike other SSMs <d-cite key="mamba,mamba2"></d-cite> that write to all memory slots at once ($r_t = \mathbf{1}$), Phoenix writes only to the slots selected by $r_t$. Unlike SWA, which fully erases the oldest slot, Phoenix gradually decays the selected slot’s previous content — preserving partial information rather than discarding it.</p> <p>For the router itself, we drew inspiration from the DeepSeek MoE family <d-cite key="deepseek_moe"></d-cite>. Each token produces a score vector via a learned linear projection, and only the top-$K$ slots are activated:</p> \[m_t = \sigma(Wx_t), \qquad g_t = \mathrm{KeepTop}_K(m_t) = \begin{cases} m_t[i], &amp; \text{if } i \in \mathrm{TopK}(m_t),\\ 0, &amp; \text{otherwise}, \end{cases}, \qquad r_t = \frac{g_t}{\alpha\sum_{i=1}^M g_t[i]}.\] <p>The decay $a_t$ follows Mamba-2’s <d-cite key="mamba2"></d-cite> scalar per-head design:</p> \[a_t = -\mathrm{SoftPlus}(w^\top x_t)\,\exp(\Delta).\] <p>And $\alpha$ normalizes the router logits — similar to the temperature scaling used in GLA <d-cite key="gla"></d-cite> — to keep the effective write magnitude stable.</p> <h2 id="design-decisions">Design Decisions</h2> <p>With the recurrence in place, we turned to the block-level design. Several choices here surprised us.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/SSM_Story/assets/img/arch-480.webp 480w,/SSM_Story/assets/img/arch-800.webp 800w,/SSM_Story/assets/img/arch-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/SSM_Story/assets/img/arch.png" width="100%" height="auto" title="block diagram" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Phoenix block design vs. other linear models block design.</figcaption> </figure> <p><strong>Dropping the short convolution.</strong> Most linear transformer blocks include a short depthwise convolution before the recurrence — a holdover that helps capture local context. We removed it. The reasoning: SWA is mathematically equivalent to an input-dependent convolution, and Phoenix’s RSM already subsumes SWA as a special case. Adding a separate convolution on top would be redundant. Similar to what Mamba-3 does, we found that removing it came at no cost and slightly simplified the architecture. The RSM, it turns out, was already doing that job implicitly.</p> <p><strong>On norms.</strong> We use QK-RMSNorm on queries and keys to stabilize training — a practice that helps prevent gradient explosions in SSMs. One nuance: in the hybrid Phoenix variant (with interleaved attention layers), we found that removing these norms actually <em>improves</em> length generalization, particularly on recall tasks. Why? Our hypothesis is that the norms interfere with position-related information that the NoPE attention layers need. It was one of those unexpected ablation results that made us rethink a long-held assumption.</p> <details><summary>Side Note on RoPE</summary> <p>In our early experiments, we included Rotary Position Embedding (RoPE) <d-cite key="rope"></d-cite> to incorporate positional information into queries and keys. However, it severely degraded the length generalization of Phoenix, one of its main strengths, so we removed it from the design.</p> </details> <p><strong>The counterintuitive choice: no load balancing.</strong> In Mixture-of-Experts models <d-cite key="moe"></d-cite>, load-balancing losses are standard practice — you push the router to distribute tokens evenly across experts to prevent a few experts from dominating. We tried this. It made Phoenix worse.</p> <p>The reason, once we thought about it, made sense: Phoenix <em>wants</em> its memory to be uneven. A slot that specializes in storing retrieval-critical tokens — passkeys, variable names, rare facts — should store <em>more</em> of those tokens, not fewer. Forcing uniform allocation would destroy exactly the specialization that makes Phoenix useful. So we dropped the load-balancing loss entirely and instead added Gumbel noise during training to encourage exploration and prevent early collapse. The router was free to specialize, and specialize it did: retrieval-critical tokens naturally clustered into dedicated slots, leaving the rest of the memory free for ordinary context.</p> <h2 id="empirical-results">Empirical Results</h2> <p>When we finally ran the experiments, we were cautiously optimistic. The theory was clean. But linear models had disappointed on recall before, and we didn’t want to overclaim.</p> <h3 id="retrieval-ability">Retrieval Ability</h3> <p>Our first question was simple: does Phoenix actually retrieve better? We pitted it against the strongest linear baselines we knew — <em>Mamba-2</em> <d-cite key="mamba2"></d-cite>, <em>GDN</em> <d-cite key="gated_deltanet"></d-cite>, and <em>GLA</em> <d-cite key="gla"></d-cite> on the SSM side, and SWA-like models including <em>SWA+RoPE</em> and <em>GSA</em> <d-cite key="gsa"></d-cite> on the other — and threw Needle-in-a-Haystack tasks at all of them.</p> <p>The degradation in the baselines was predictable once you understood the mechanism: Mamba-2 and GDN write every token to every slot, so over a long sequence, each slot becomes a blurry average of everything it has ever seen. By 8K tokens — already $4\times$ their training length — the passkey signal is too diluted to recover.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/SSM_Story/assets/img/recall-480.webp 480w,/SSM_Story/assets/img/recall-800.webp 800w,/SSM_Story/assets/img/recall-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/SSM_Story/assets/img/recall.png" width="100%" height="auto" title="tab recall" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">In-Context Recall Benchmarks and NIAH Accuracy vs. Context Length and Cache Size. We report accuracy (%) on SWDE, FDA, and SQuAD, as well as on individual NIAH-1, NIAH-2, and NIAH-3 tasks across varying context lengths. Rec. mem. and Conv. mem. denote the millions of cached state elements used during decoding.</figcaption> </figure> <p>Phoenix behaved differently. It held near-perfect accuracy ($\geq \mathbf{99\%}$) all the way to 16K tokens, and remained the <em>only model</em> at the 400M scale to keep strong performance ($&gt; \mathbf{91\%}$) at 32K — that’s $\mathbf{16\times}$ its training length. The passkey was going into a dedicated slot at the moment it was encountered, and staying there. No dilution. No overwriting.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/SSM_Story/assets/img/niah-480.webp 480w,/SSM_Story/assets/img/niah-800.webp 800w,/SSM_Story/assets/img/niah-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/SSM_Story/assets/img/niah.png" width="100%" height="auto" title="niah" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">NIAH-1 accuracy visualization vs sequence length. Dashed line indicate training sequence length.</figcaption> </figure> <h3 id="language-modeling">Language Modeling</h3> <p>A natural worry at this point: did we buy recall ability at the cost of general language modeling? We had deliberately made the memory uneven — would that hurt perplexity or zero-shot benchmarks?</p> <p>It didn’t. Across standard evaluations, Phoenix matched or surpassed <strong>Mamba-2</strong> <d-cite key="mamba2"></d-cite>, <strong>GLA</strong> <d-cite key="gla"></d-cite>, and <strong>GDN</strong> <d-cite key="gated_deltanet"></d-cite>, as well as strong Transformer baselines like <em>FoX</em>, at both <strong>400M and 800M parameter scales</strong>. It even achieved the <strong>best Lambada performance at 400M</strong>. Selective memory, it turns out, doesn’t hurt general language modeling — if anything, routing tokens to content-appropriate slots seems to help.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/SSM_Story/assets/img/lm-480.webp 480w,/SSM_Story/assets/img/lm-800.webp 800w,/SSM_Story/assets/img/lm-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/SSM_Story/assets/img/lm.png" width="100%" height="auto" title="niah" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Zero-shot language modeling performance across models.</figcaption> </figure> <h3 id="hybrid-phoenix">Hybrid Phoenix</h3> <p>We also experimented with a hybrid variant — interleaving Phoenix RSM layers with standard attention layers. The results here were striking. The hybrid Phoenix achieved near-perfect NIAH-1 accuracy up to <strong>32K tokens</strong> and strong NIAH-2 performance up to <strong>16K tokens</strong>, while other SSM hybrids like GDN+Attn <d-cite key="gated_deltanet"></d-cite> and Mamba-2+Attn <d-cite key="mamba2"></d-cite> started breaking down at 4K and 2K respectively. The combination of NoPE attention — good at precise short-range retrieval — with Phoenix’s persistent, content-addressed slots turned out to be unusually complementary.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/SSM_Story/assets/img/hybrid-480.webp 480w,/SSM_Story/assets/img/hybrid-800.webp 800w,/SSM_Story/assets/img/hybrid-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/SSM_Story/assets/img/hybrid.png" width="100%" height="auto" title="hybrid" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Recall ability of hybrid-phoenix vs other hybrid architechtures.</figcaption> </figure> <h2 id="a-pleasant-surprise">A Pleasant Surprise</h2> <p>We had set out to fix recall. We hadn’t planned on fixing length generalization. But there it was.</p> <p>During evaluation, we noticed Phoenix generalizing to sequences far beyond its training length — well beyond what any SSM had demonstrated before. We hadn’t engineered this; it seemed to be a byproduct of the routing mechanism. So we went looking for an explanation, and that search led us to <a href="https://www.linkedin.com/in/ricardobuitrago">Ricardo</a>, who helped us develop the concept of <strong>Effective Sequence Length (ESL)</strong>.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/SSM_Story/assets/img/esl_1-480.webp 480w,/SSM_Story/assets/img/esl_1-800.webp 800w,/SSM_Story/assets/img/esl_1-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/SSM_Story/assets/img/esl_1.png" width="100%" height="auto" title="esl" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Normalized effective sequence length for a NIAH-1 sample at sequence length 16K. SWA stores each token in exactly one slot (FIFO). Phoenix shows the hidden state $S_t$ for layer 1, head 1 (256 slots, Top$_{32}$). SSM stores each token in all slots with decay. Slots are reordered by usage frequency; results correspond to 400M parameter models.</figcaption> </figure> <p>The insight is simple in retrospect. In a standard SSM, every slot sees every token — the effective sequence length is $T$ for all slots. In SWA, every slot sees exactly $\frac{T}{M}$ tokens — uniform and predictable. In Phoenix, the router creates <em>diversity</em>: some slots see many tokens (acting like SSM slots), while others see very few — just the rare tokens they’ve specialized for. This diversity means no single slot consistently overfits to the training sequence length, which appears to be why Phoenix generalizes so gracefully beyond it.</p> <p>To make this concrete, we looked directly inside Phoenix’s hidden state on a synthetic NIAH task. The picture was striking:</p> <figure style="text-align: center;"> <img src="/SSM_Story/assets/video/best.gif" alt="Phoenix Memory Dynamics — animated visualization of memory slot allocation across two heads" style="width: 80%; max-width: 700px;"/> <figcaption style="margin-top: 0.6em; font-size: 0.85em; color: #555; line-height: 1.5;"> <strong>Phoenix Memory Dynamics.</strong> Memory allocation for two different heads of Phoenix on a synthetic NIAH-style task. <span style="color:#c0392b;">■</span> Red slots store tokens important for retrieval (e.g., passwords), <span style="color:#27ae60;">■</span> green slots store non-retrieval tokens, and <span style="color:#2980b9;">■</span> blue slots are shared memory between the two types of tokens. Different heads allocate different amounts of slots to retrieval-important tokens, illustrating non-uniform memory allocation across heads. </figcaption> </figure> <p>The passkey — shown in $\color{red}{red}$ — goes into a dedicated region of the hidden state the moment it appears, and stays there. Ordinary tokens shown in $\color{green}{green}$ flow through the general-purpose slots without touching it. Some slots shown in $\color{blue}{blue}$ serve both roles. Crucially, different heads allocate different amounts of memory to retrieval-critical tokens — the model discovered this specialization entirely on its own, without any explicit supervision.</p> <p>This is what “organizing memory like a closet” looks like from the inside.</p> <h2 id="final-notes-and-future">Final Notes and Future</h2> <p>Phoenix started as an answer to a simple frustration — linear models that forgot too easily — and ended up revealing something deeper about how memory can be organized in learned sequence models. The routing idea is not complicated, but its consequences are: better recall, better language modeling, and a surprising bonus of length generalization, all from the same mechanism.</p> <p>There is plenty left to explore. How far can the length generalization be pushed? Can the routing mechanism be made even more expressive? What happens at truly large scales? We don’t have all the answers yet — but we’re working on it.</p> <hr/>]]></content><author><name>Arshia Afzal$^*$ (Bloggin 🎙️)</name></author><summary type="html"><![CDATA[Architecture and Results]]></summary></entry><entry><title type="html">Story of Linear Time Sequence Modeling 📚</title><link href="https://arshiaafzal.github.io/SSM_Story/2025/lion-part3-chunk/" rel="alternate" type="text/html" title="Story of Linear Time Sequence Modeling 📚"/><published>2025-08-29T00:00:00+00:00</published><updated>2025-08-29T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/SSM_Story/2025/lion-part3-chunk</id><content type="html" xml:base="https://arshiaafzal.github.io/SSM_Story/2025/lion-part3-chunk/"><![CDATA[<hr/> <h1 id="why-linear-transformers">Why Linear Transformers?</h1> <p>If you’ve heard about Large Language Models (LLMs) and Transformers and are curious to learn more, there are already plenty of excellent blog posts, articles, and YouTube videos that explain them in great detail with amazing visualizations. In case your main interest is understanding LLMs and how Transformers work, we’d point you there first, they’ve done a fantastic job (honestly, better than us).</p> <p>In this series, we’ll cover Linear Transformers and State Space Models (SSMs), giving a high-level summary of their core ideas. So, if you’ve come across names like <strong>Mamba</strong> or <strong>DeltaNet</strong> and wondered what they are, or if you’ve asked yourself:</p> <p><strong>1)</strong> Why we moved from RNNs to Transformers?</p> <p><strong>2)</strong> Why we now seem to be circling back from Transformers toward Linear Transformers (almost like “RNNs on steroids”)?</p> <p>then this post should be a good fit 😉.</p> <p>So let’s start from answering the above questions</p> <hr/>]]></content><author><name>Arshia</name></author><summary type="html"><![CDATA[Summary of Linear Transformers]]></summary></entry><entry><title type="html">Story of Linear Time Sequence Modeling 📚</title><link href="https://arshiaafzal.github.io/SSM_Story/2025/lion-part4-results/" rel="alternate" type="text/html" title="Story of Linear Time Sequence Modeling 📚"/><published>2025-08-29T00:00:00+00:00</published><updated>2025-08-29T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/SSM_Story/2025/lion-part4-results</id><content type="html" xml:base="https://arshiaafzal.github.io/SSM_Story/2025/lion-part4-results/"><![CDATA[<hr/> <h1 id="why-linear-transformers">Why Linear Transformers?</h1> <p>If you’ve heard about Large Language Models (LLMs) and Transformers and are curious to learn more, there are already plenty of excellent blog posts, articles, and YouTube videos that explain them in great detail with amazing visualizations. In case your main interest is understanding LLMs and how Transformers work, we’d point you there first, they’ve done a fantastic job (honestly, better than us).</p> <p>In this series, we’ll cover Linear Transformers and State Space Models (SSMs), giving a high-level summary of their core ideas. So, if you’ve come across names like <strong>Mamba</strong> or <strong>DeltaNet</strong> and wondered what they are, or if you’ve asked yourself:</p> <p><strong>1)</strong> Why we moved from RNNs to Transformers?</p> <p><strong>2)</strong> Why we now seem to be circling back from Transformers toward Linear Transformers (almost like “RNNs on steroids”)?</p> <p>then this post should be a good fit 😉.</p> <p>So let’s start from answering the above questions</p> <hr/>]]></content><author><name>Arshia</name></author><summary type="html"><![CDATA[Summary of Linear Transformers]]></summary></entry></feed>