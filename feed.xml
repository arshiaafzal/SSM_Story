<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://arshiaafzal.github.io/SSM_Story/feed.xml" rel="self" type="application/atom+xml"/><link href="https://arshiaafzal.github.io/SSM_Story/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-05T17:51:03+00:00</updated><id>https://arshiaafzal.github.io/SSM_Story/feed.xml</id><title type="html">blank</title><subtitle></subtitle><entry><title type="html">Phoenix (Part-1)</title><link href="https://arshiaafzal.github.io/SSM_Story/2025/lion-part1-model/" rel="alternate" type="text/html" title="Phoenix (Part-1)"/><published>2025-08-29T00:00:00+00:00</published><updated>2025-08-29T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/SSM_Story/2025/lion-part1-model</id><content type="html" xml:base="https://arshiaafzal.github.io/SSM_Story/2025/lion-part1-model/"><![CDATA[<div style="background:#f8f9fa; border-left:3px solid #adb5bd; padding:0.6rem 1rem; margin-bottom:1.5rem; border-radius:0 4px 4px 0; font-size:0.9rem;"> <strong>Phoenix Series</strong> &nbsp;—&nbsp; <strong>Part 1: Memory as a set of Slots</strong> &nbsp;|&nbsp; <a href="/SSM_Story/2025/lion-part2-theory/">Part 2: Architecture and Results →</a> </div> <h2 id="the-recall-gap">The Recall Gap</h2> <p>Mamba <d-cite key="mamba"></d-cite> showed that attention <d-cite key="attention"></d-cite> is not strictly necessary for strong sequence modeling. A simple recurrent model could match, and sometimes outperform, Transformers on language modeling at a fraction of the cost. But there is no free lunch. Soon after, several works exposed a critical limitation: fixed-size memory models struggle on recall benchmarks, which is not surprising, since they have only a finite amount of room to store information. While it’s tempting to blame their finite capacity, the reality is more nuanced.</p> <p>Consider Needle-In-The-Haystack (NIAH). The model is first given a lookup key, then a long dictionary, and finally asked to retrieve the matching value. This is a form of <strong>constant-memory recall</strong>: in principle, it requires only $O(1)$ memory, since the model only needs to retain the key and its corresponding value as it scans the sequence (unlike <strong>scaling-memory recall</strong> tasks like KV-Retrieval, which require $O(T)$ memory because the query comes after the dictionary).</p> <p>Despite this theoretical $O(1)$ requirement, recurrent models still lag behind Transformers on NIAH. This failure isn’t just a quirk of synthetic benchmarks; it mirrors real-world bottlenecks. In multiple-choice questions, a model may identify the correct answer early on but fail to preserve that single letter in its state until the output <d-cite key="gna"></d-cite>. Similarly, in math and code, models struggle to track a single variable or a few intermediate values over long distances.</p> <p>The issue, then, isn’t a lack of physical room—it’s a lack of <strong>persistence</strong>: the ability to leave specific information untouched. The model has the capacity; it just lacks the organization.</p> <h2 id="organize-your-memory-like-your-closet">Organize your memory like your closet 👕</h2> <blockquote> <p><em>Can we design a recurrent model with strong constant-memory recall that approach their theoretical limits?</em></p> </blockquote> <p>To understand why persistence is important, think of your hidden state as a closet. When you buy a new piece of clothing, you don’t shove it into a giant pile on the floor, nor do you throw out your favorite jacket just to fit a new shirt. Instead, you take two deliberate steps:</p> <p><strong>1. <em>Decide which shelf it belongs on.</em></strong> (Routing) <br/> <strong>2. <em>Reorganize that shelf to make room.</em></strong> (Squeezing / Decay)</p> <p>In this analogy, the <strong>closet</strong> is the memory, the <strong>shelves</strong> are memory slots, and tokens are the <strong>clothes</strong>. Current models fail because they only perform one of these steps effectively.</p> <h3 id="ssms-closet-is-a-pile-of-clothes">SSMs’ closet is a pile of clothes</h3> <p>State space models (SSMs) <d-cite key="mamba,mamba2"></d-cite> and linear transformers <d-cite key="linearattn"></d-cite> all share the same fundamental memory operation — a matrix-valued hidden state $S_t \in \mathbb{R}^{M\times d}$ updated by a <strong>linear time-dependent</strong> recurrence:</p> \[S_t = \underbrace{S_{t-1}A_t}_{\text{Decay}} + \underbrace{v_t k_t^\top}_{\text{Write}}, \qquad o_t = \underbrace{S_t q_t}_{\text{Read}}\] <p>SSMs are masters of the <strong>reorganizing</strong> step. The forget gate $A_t$ acts as a mechanism for decaying old content to make room for the new. However, they lack the ability to choose a shelf.</p> <p>In the diagonal case ($A_t=\text{diag}(a_t)$), the update simplifies to $S_t = S_{t-1} \odot a_t + v_t k_t^\top$. Here, the scalar $a_t$ squeezes information uniformly across the entire state—like compressing every shelf in the closet simultaneously. Because the write $v_t k_t^\top$ lands across all $M$ rows, every new token dilutes everything previously stored. There is no isolation; there is only the pile.</p> <div style="margin: 1.5rem auto 1rem; display: flex; justify-content: center; width: 100%;"> <iframe id="ssm-recurrent-matrix-update" src="/SSM_Story/assets/html/ssm_recurrent_matrix_update.html" title="SSM recurrent matrix update visualization" loading="lazy" scrolling="no" style="display: block; width: 70%; max-width: 1400px; height: 0; border: 0; border-radius: 0rem; background: transparent; overflow: hidden;"></iframe> </div> <h3 id="swas-closet-throws-out-the-old-clothes">SWA’s closet throws out the old clothes</h3> <p>Sliding window attention (SWA) takes the opposite approach. It maintains a fixed cache and, at each step, $\color{red}{\text{drops}}$ the oldest token to $\color{green}{\text{append}}$ the newest one. Written as a matrix update using a one-hot selector $e_t$:</p> \[[S^{k}_t, S^v_t] = \underbrace{(\mathbf{1} - e_t)}_{\color{red}{\text{Remove oldest}}}\odot[S^{k}_{t-1}, S^v_{t-1}] + \underbrace{e_t}_{\color{green}{\text{Write new}}} [k_t^\top, v_t^\top]\] <p>SWA is great at choosing a shelf; it writes to exactly one slot ($e_t$). But it cannot reorganize. It doesn’t squeeze old content to make room; it deletes it ruthlessly to make space for the new.</p> <div style="margin: 1.5rem auto 1rem; display: flex; justify-content: center; width: 100%;"> <iframe id="swa-recurrent-matrix-update" src="/SSM_Story/assets/html/swa_recurrent_matrix_update.html" title="SWA recurrent matrix update visualization" loading="lazy" scrolling="no" style="display: block; width: 70%; max-width: 1400px; height: 0; border: 0; border-radius: 0rem; background: transparent; overflow: hidden;"></iframe> </div> <h3 id="the-missing-piece">The Missing Piece</h3> <p>If SSMs write blindly and SWA deletes ruthlessly, the solution is a model that can choose where to write based on what it is seeing.</p> <p>Bridging the recall gap requires a single mechanism that can do both: treat subsets of the hidden state as independent slots that can be selectively updated or gracefully preserved. This dual requirement—routing and decay—is the foundation of the framework.</p> <h2 id="routing-slot-memories">Routing Slot Memories</h2> <p>Instead of SWA’s fixed ring-buffer ($e_t$), what if we used a learned, sparse router vector $r_t \in \mathbb{R}^M$? This router looks at the current token and picks exactly which “shelves” it belongs on. This is the core of Routing Slot Memories (RSMs):</p> \[S_t = \underbrace{(\mathbf{1} - r_t) \odot S_{t-1}}_{\text{Preserved Shelves}} + \underbrace{r_t \odot \bigl(D_t\,S_{t-1}\,A_t + U_t\bigr)}_{\text{Updated Shelves}}\] <p>The beauty of this formulation is that it’s a spectrum. Because $r_t$ is sparse, most entries are zero—meaning most of the memory is left completely untouched.</p> <ul> <li>The First Term $(\mathbf{1} - r_t) \odot S_{t-1}$ is the persistence mechanism. It ensures that any shelf not selected by the router remains perfectly preserved.</li> <li>The Second Term $r_t \odot (\dots)$ is the selective update. Only the chosen shelves undergo the “squeezing” ($D_t, A_t$) and “writing” ($U_t$) process.</li> </ul> <p>By simply changing the behavior of the router $r_t$, this single equation recovers the entire landscape of current sequence models:x</p> <div style="margin: 1.5rem auto 1rem; display: flex; justify-content: center; width: 100%; overflow-x: auto;"> <iframe id="comparison-table" src="/SSM_Story/assets/html/comparison_table.html" title="Comparison table" loading="lazy" scrolling="no" style="display: block; width: 100%; max-width: 100%; height: 0; border: 0; border-radius: 0rem; background: transparent; overflow: hidden;"></iframe> </div> <p>A dense router ($r_t = \mathbf{1}$) gives you an SSM—every token hits every shelf. A one-hot cyclic router ($r_t = e_t$) gives you SWA—every token hits the oldest shelf.</p> <p>The question then became concrete: Can we train a model to learn, purely from data, which shelf each token belongs on—and keep it there?</p> <p>Our answer is <strong>Phoenix</strong>. By using a <strong>learned sparse router</strong>, Phoenix treats its hidden state like a Mixture-of-Experts <d-cite key="moe"></d-cite> for memory. Much like an organized closet, it can tuck a needle into a dedicated “retrieval shelf” and leave it undisturbed by the thousands of “filler” tokens flowing through other slots.</p> <p>In <strong>Part 2</strong>, we’ll move from theory to practice: we’ll look at the specific architecture of the Phoenix block, the “counterintuitive” design decisions that made it work, and how this organized memory allows it to recall information $16\times$ beyond its training length.</p> <script>
  (() => {
    const iframe = document.getElementById('swa-recurrent-matrix-update');
    if (!iframe) return;

    const resizeIframe = () => {
      try {
        const doc = iframe.contentDocument || iframe.contentWindow?.document;
        if (!doc || !doc.body || !doc.documentElement) return;

        const height = Math.max(
          doc.body.scrollHeight,
          doc.documentElement.scrollHeight,
          doc.body.offsetHeight,
          doc.documentElement.offsetHeight
        );

        iframe.style.height = `${height}px`;
      } catch (error) {
        // Ignore cross-document timing issues during initial load.
      }
    };

    iframe.addEventListener('load', () => {
      resizeIframe();
      window.setTimeout(resizeIframe, 0);
      window.setTimeout(resizeIframe, 250);
      window.setTimeout(resizeIframe, 1000);
    });

    window.addEventListener('resize', resizeIframe);
  })();
</script> <script>
  (() => {
    const iframe = document.getElementById('ssm-recurrent-matrix-update');
    if (!iframe) return;

    const resizeIframe = () => {
      try {
        const doc = iframe.contentDocument || iframe.contentWindow?.document;
        if (!doc || !doc.body || !doc.documentElement) return;

        const height = Math.max(
          doc.body.scrollHeight,
          doc.documentElement.scrollHeight,
          doc.body.offsetHeight,
          doc.documentElement.offsetHeight
        );

        iframe.style.height = `${height}px`;
      } catch (error) {
        // Ignore cross-document timing issues during initial load.
      }
    };

    iframe.addEventListener('load', () => {
      resizeIframe();
      window.setTimeout(resizeIframe, 0);
      window.setTimeout(resizeIframe, 250);
      window.setTimeout(resizeIframe, 1000);
    });

    window.addEventListener('resize', resizeIframe);
  })();
</script> <script>
  (() => {
    const iframe = document.getElementById('comparison-table');
    if (!iframe) return;

    const resizeIframe = () => {
      try {
        const doc = iframe.contentDocument || iframe.contentWindow?.document;
        if (!doc || !doc.body || !doc.documentElement) return;

        const height = Math.max(
          doc.body.scrollHeight,
          doc.documentElement.scrollHeight,
          doc.body.offsetHeight,
          doc.documentElement.offsetHeight
        );

        iframe.style.height = `${height}px`;
      } catch (error) {
        // Ignore cross-document timing issues during initial load.
      }
    };

    iframe.addEventListener('load', () => {
      resizeIframe();
      window.setTimeout(resizeIframe, 0);
      window.setTimeout(resizeIframe, 250);
      window.setTimeout(resizeIframe, 1000);
    });

    window.addEventListener('resize', resizeIframe);
  })();
</script>]]></content><author><name>Arshia Afzal$^*$</name></author><summary type="html"><![CDATA[Memory as a set of Slots]]></summary></entry><entry><title type="html">Phoenix (Part-2)</title><link href="https://arshiaafzal.github.io/SSM_Story/2025/lion-part2-theory/" rel="alternate" type="text/html" title="Phoenix (Part-2)"/><published>2025-08-29T00:00:00+00:00</published><updated>2025-08-29T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/SSM_Story/2025/lion-part2-theory</id><content type="html" xml:base="https://arshiaafzal.github.io/SSM_Story/2025/lion-part2-theory/"><![CDATA[<div style="background:#f8f9fa; border-left:3px solid #adb5bd; padding:0.6rem 1rem; margin-bottom:1.5rem; border-radius:0 4px 4px 0; font-size:0.9rem;"> <strong>Phoenix Series</strong> &nbsp;—&nbsp; <a href="/SSM_Story/2025/lion-part1-model/">← Part 1: Memory as a set of Slots</a> &nbsp;|&nbsp; <strong>Part 2: Architecture and Results</strong> </div> <h2 id="from-framework-to-model">From Framework to Model</h2> <p>In Part 1, we introduced Routing Slot Memories, a framework that combines the selective writing of SWA with the gradual forgetting of SSMs. The next step was to turn that framework into a concrete model.</p> <p>RSM is deliberately general. To instantiate Phoenix, one needs to choose how the router $r_t$ is computed and how the decay term $a_t$ is parameterized. We wanted a design expressive enough to learn meaningful slot assignments, but simple enough to train stably at scale.</p> <p>Our recurrence uses separate key and value states, mirroring SWA, but replaces hard deletion with learned decay:</p> <div style="background:#eff6ff; border-radius:10px; padding:0.5rem 1.5rem; margin:0.5rem 0;"> $$ S^k_t = (1-\exp(a_tr_t)) \odot S^k_{t-1} + \exp(a_tr_t)^\top k_t $$ $$ S^v_t = (1-\exp(a_tr_t)) \odot S^v_{t-1} + \exp(a_tr_t)^\top v_t $$ </div> <p>Unlike standard SSMs <d-cite key="mamba,mamba2"></d-cite> that write to all memory slots at once ($r_t = \mathbf{1}$), Phoenix writes only to the slots selected by $r_t$. And unlike SWA, which fully overwrites the evicted slot, Phoenix decays the previous content of the selected slot rather than removing it outright.</p> <p>For routing, we draw inspiration from the DeepSeek Mixture-of-Experts (MoE) family <d-cite key="deepseek_moe"></d-cite>. Each token is projected to a score vector, and only the top-$K$ entries remain active:</p> <div style="background:#eff6ff; border-radius:10px; padding:0.5rem 1.5rem; margin:0.5rem 0;"> $$ \begin{aligned} m_t &amp;= \sigma(Wx_t), \\ g_t &amp;= \mathrm{KeepTop}_K(m_t) = \begin{cases} m_t[i], &amp; \text{if } i \in \mathrm{TopK}(m_t),\\ 0, &amp; \text{otherwise}. \end{cases} \end{aligned} $$ </div> <p>By isolating only the highest-scoring slots, the memory dynamically specializes based on the content of the token. Finally, to keep the effective write magnitude stable, these gated scores are normalized into the final routing weights:</p> <div style="background:#eff6ff; border-radius:10px; padding:0.5rem 1.5rem; margin:0.5rem 0;"> $$ r_t = \frac{g_t}{\alpha\sum_{i=1}^M g_t[i]}. $$ </div> <p>Here, $\alpha$ controls the effective write scale, similarly to the role of temperature-like scaling in GLA .</p> <div style="margin: 1.5rem auto 1rem; display: flex; justify-content: center; width: 100%;"> <iframe id="phoenix-recurrent-matrix-update" src="/SSM_Story/assets/html/phoenix_recurrent_matrix_update.html" title="Phoenix recurrent matrix update visualization" loading="lazy" scrolling="no" style="display: block; width: 70%; max-width: 1400px; height: 0; border: 0; border-radius: 0rem; background: transparent; overflow: hidden;"></iframe> </div> <h2 id="design-decisions">Design Decisions</h2> <p>With the recurrence fixed, we turned to the block design.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/SSM_Story/assets/img/arch-480.webp 480w,/SSM_Story/assets/img/arch-800.webp 800w,/SSM_Story/assets/img/arch-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/SSM_Story/assets/img/arch.png" class="rounded mx-auto d-block" width="60%" height="auto" style=" max-width: 60%; " title="block diagram" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> <figcaption class="caption">Phoenix block design vs. other linear models block design.</figcaption> </figure> <p><strong>Dropping the short convolution.</strong> Most linear transformer blocks include a short depthwise convolution before the recurrence — a holdover that helps capture local context. We removed it. The reasoning: SWA is mathematically equivalent to an input-dependent convolution, and Phoenix’s RSM already subsumes SWA as a special case. Adding a separate convolution on top would be redundant. </p> <p><strong>On norms.</strong> We apply QK-RMSNorm to queries and keys to stabilize training and avoid gradient explosions, following common practice in SSMs. One interesting exception appears in hybrid Phoenix, where Phoenix layers are interleaved with attention. In that setting, removing these norms improves length generalization, especially on recall tasks. We hypothesize that the norms suppress position-related information that the NoPE attention layers rely on. This was one of the more unexpected ablation results.</p> <details><summary>Side Note on RoPE</summary> <p>In early experiments, we applied Rotary Position Embedding (RoPE) <d-cite key="rope"></d-cite> to queries and keys. This substantially hurt Phoenix’s length generalization, which is one of its main strengths, so we removed it from the final design.</p> </details> <p><strong>No load balancing.</strong> In Mixture-of-Experts models, load-balancing losses are commonly used to spread tokens more evenly across experts. We do not use such a loss in Phoenix, since its memory is most useful when allocation is uneven. Slots that specialize in retrieval-critical tokens, such as passkeys, variable names, or rare facts, should receive more of those tokens rather than be pushed toward uniform usage. For this reason, we allow the router to specialize freely, and use Gumbel noise during training only to encourage exploration and avoid early collapse. This intuition is supported by our experiments: retrieval-critical tokens naturally cluster into dedicated slots, while other slots remain available for ordinary context.</p> <h2 id="empirical-results">Empirical Results</h2> <p>We now turn to the empirical evaluation. Our main question was whether Phoenix improves in-context retrieval without sacrificing general language modeling.</p> <h3 id="retrieval-ability">Retrieval Ability</h3> <p>We begin with retrieval. We compare Phoenix against <em>Mamba-2</em> <d-cite key="mamba2"></d-cite>, <em>GDN</em> <d-cite key="gated_deltanet"></d-cite>, and <em>GLA</em> <d-cite key="gla"></d-cite> on the SSM side, and SWA-like models including <em>SWA+RoPE</em> and <em>GSA</em> <d-cite key="gsa"></d-cite> on the other — and threw Needle-in-a-Haystack tasks at all of them.</p> <p>The baseline behavior is consistent with their memory mechanism: Mamba-2 and GDN write every token to every slot, so over a long sequence, each slot becomes a blurry average of everything it has ever seen. By 8K tokens — already $4\times$ their training length — the passkey signal is too diluted to recover.</p> <div style="margin: 2rem auto; display: flex; flex-direction: column; gap: 0.4rem; align-items: center; max-width: 100%;"> <div style="width: 100%; display: flex; justify-content: center; overflow-y: hidden;"> <iframe id="recall-table" src="/SSM_Story/assets/html/recall_table.html" title="Benchmark table" loading="lazy" scrolling="no" style="display: block; width: 100%; height: 420px; border: 0; background: transparent;" onload="(function(f){try{const d=f.contentWindow.document;const resize=function(){f.style.height='0px';const h=Math.max(d.body?d.body.scrollHeight:0,d.documentElement?d.documentElement.scrollHeight:0);f.style.height=(h+2)+'px';};resize();setTimeout(resize,120);setTimeout(resize,500);}catch(e){}})(this)"></iframe> </div> <div style="width: 100%; font-size: 0.85rem; color: #475569; line-height: 1.6; text-align: left;"> <strong style="color: #1e293b; display: block; margin-bottom: 0.5rem;">Table 2: In-context recall benchmarks and NIAH accuracy vs. context length and cache size.</strong> We report accuracy (%) on SWDE/FDA/SQuAD and on single NIAH-1/2/3 across context lengths. Rec. mem. and Conv. mem. denote the millions of cached state elements used during decoding. The common baseline of \(12.5\)M recurrent elements corresponds to a 24-layer model with a total state size of \(0.57\)M per layer (derived from \(2\times\) factors in architectures like GSA/SWA or larger single tensors in Mamba-2/GDN). </div> </div> <p>Phoenix behaved differently. It held near-perfect accuracy ($\geq \mathbf{99\%}$) all the way to 16K tokens, and remained the <em>only model</em> at the 400M scale to keep strong performance ($&gt; \mathbf{91\%}$) at 32K — that’s $\mathbf{16\times}$ its training length. This behavior is consistent with the routing mechanism: the passkey can be written into a dedicated slot and remain recoverable over long spans, rather than being diluted by subsequent tokens.</p> <div style="margin: 1.5rem -8% 0; width: 116%;"> <iframe id="niah-plot" src="/SSM_Story/assets/html/niah_plot.html" title="NIAH-1 accuracy interactive plot" loading="lazy" scrolling="no" style="display: block; width: 100%; height: 490px; border: 0; border-radius: 0.5rem; background: transparent;" onload="(function(f){try{var d=f.contentWindow.document;var r=function(){f.style.height='0px';var h=Math.max(d.body?d.body.scrollHeight:0,d.documentElement?d.documentElement.scrollHeight:0);f.style.height=(h+4)+'px';};r();setTimeout(r,150);setTimeout(r,600);}catch(e){}})(this)"></iframe> </div> <h3 id="language-modeling">Language Modeling</h3> <p>A natural question is whether this improvement in retrieval comes at the expense of general language modeling. We had deliberately made the memory uneven — would that hurt perplexity or zero-shot benchmarks?</p> <p>Across standard evaluations, Phoenix matched or surpassed <strong>Mamba-2</strong> <d-cite key="mamba2"></d-cite>, <strong>GLA</strong> <d-cite key="gla"></d-cite>, and <strong>GDN</strong> <d-cite key="gated_deltanet"></d-cite>, as well as strong Transformer baselines like <em>FoX</em>, at both <strong>400M and 800M parameter scales</strong>. It even achieved the <strong>best Lambada performance at 400M</strong>. These results suggest that selective memory does not compromise general language modeling, and may even help by routing tokens to more appropriate slots.</p> <div style="margin: 2rem auto; display: flex; flex-direction: column; gap: 0.4rem; align-items: center; max-width: 100%;"> <div style="width: 100%; display: flex; justify-content: center; overflow-y: hidden;"> <iframe id="lm-table" src="/SSM_Story/assets/html/lm_table.html" title="Benchmark table" loading="lazy" scrolling="no" style="display: block; width: 100%; height: 420px; border: 0; background: transparent;" onload="(function(f){try{const d=f.contentWindow.document;const resize=function(){f.style.height='0px';const h=Math.max(d.body?d.body.scrollHeight:0,d.documentElement?d.documentElement.scrollHeight:0);f.style.height=(h+2)+'px';};resize();setTimeout(resize,120);setTimeout(resize,500);}catch(e){}})(this)"></iframe> </div> <div style="width: 100%; font-size: 0.85rem; color: #475569; line-height: 1.6; text-align: left;"> <strong style="color: #1e293b; display: block; margin-bottom: 0.5rem;">Table 3: Language modeling and zero-shot evaluation results. </strong> Perplexity on Lambada (LMB.) and zero-shot accuracy on Lambada, PIQA, HellaSwag, WinoGrande, ARC-e, and ARC-c for Transformer, SSM, and Phoenix models at 400M and 800M parameter scales. Phoenix matches strong linear baselines overall while maintaining strong downstream performance. </div> </div> <h3 id="hybrid-phoenix">Hybrid Phoenix</h3> <p>We also experimented with a hybrid variant — interleaving Phoenix RSM layers with standard attention layers. The results here were striking. The hybrid Phoenix achieved near-perfect NIAH-1 accuracy up to <strong>32K tokens</strong> and strong NIAH-2 performance up to <strong>16K tokens</strong>, while other SSM hybrids like GDN+Attn <d-cite key="gated_deltanet"></d-cite> and Mamba-2+Attn <d-cite key="mamba2"></d-cite> started breaking down at 4K and 2K respectively. The combination of NoPE attention — good at precise short-range retrieval — with Phoenix’s persistent, content-addressed slots turned out to be unusually complementary.</p> <div style="margin: 2rem 0; width: 100%; display: flex; flex-direction: column; gap: 0.75rem; align-items: stretch;"> <div style="width: 100%; overflow: hidden;"> <iframe id="hybrid-table" src="/SSM_Story/assets/html/hybrid_table.html" title="Benchmark table" loading="lazy" scrolling="no" style="display: block; width: 100%; max-width: 100%; height: 420px; border: 0; background: transparent;" onload="(function(f){try{const d=f.contentWindow.document;const resize=function(){f.style.height='0px';const h=Math.max(d.body?d.body.scrollHeight:0,d.documentElement?d.documentElement.scrollHeight:0);f.style.height=(h+2)+'px';};resize();setTimeout(resize,120);setTimeout(resize,500);}catch(e){}})(this)"></iframe> </div> <div style="width: 100%; font-size: 0.85rem; color: #475569; line-height: 1.6; text-align: left;"> <strong style="color: #1e293b; display: block; margin-bottom: 0.5rem;">Table 4: Hybrid Models Retrieval Ability.</strong> Recall ability of hybrid-phoenix vs other hybrid architechtures. </div> </div> <h2 id="length-generalization">Length Generalization</h2> <p>One surprising outcome was Phoenix’s strong length generalization. During evaluation, we found that Phoenix remained effective far beyond its training length, beyond what had previously been observed in SSM-based models. This behavior was not explicitly designed for, which motivated us to look for a mechanistic explanation. In developing that explanation, we found it useful to think in terms of <strong>Effective Sequence Length (ESL)</strong> (ESL), a concept that came out of our discussion with <a href="https://r-buitrago.github.io/">Ricardo</a>.</p> <div style="margin: 0.6rem auto 2rem; display: flex; flex-direction: column; gap: 0.4rem; align-items: center; max-width: 100%;"> <div style="width: 100%; display: flex; justify-content: center; overflow-y: hidden;"> <iframe id="esl-table" src="/SSM_Story/assets/html/esl_figure.html" title="Benchmark table" loading="lazy" scrolling="no" style="display: block; width: 100%; height: 600px; border: 0; background: transparent;" onload="(function(f){try{const d=f.contentWindow.document;const resize=function(){f.style.height='0px';const h=Math.max(d.body?d.body.scrollHeight:0,d.documentElement?d.documentElement.scrollHeight:0);f.style.height=(h+2)+'px';};resize();setTimeout(resize,120);setTimeout(resize,500);}catch(e){}})(this)"></iframe> </div> <div style="width: 100%; font-size: 0.85rem; color: #475569; line-height: 1.6; text-align: left;"> <strong style="color: #1e293b; display: block; margin-bottom: 0.5rem;">Figure: Effective Sequence Length.</strong> Normalized effective sequence length for a NIAH-1 sample at sequence length 16K. SWA stores each token in exactly one slot (FIFO). Phoenix shows the hidden state $S_t$ for layer 1, head 1 (256 slots, Top$_{32}$). SSM stores each token in all slots with decay. Slots are reordered by usage frequency; results correspond to 400M parameter models. </div> </div> <p>In a standard SSM, every slot receives every token, so the effective sequence length of each slot is $T$. In SWA, each slot receives exactly $\frac{T}{M}$ tokens, yielding uniform allocation across slots. In Phoenix, routing produces a more heterogeneous pattern: some slots receive many tokens, while others receive only a small subset, including rare retrieval-critical tokens. As a result, different slots operate over different effective sequence lengths, which may help explain Phoenix’s improved length generalization.</p> <p>To make this concrete, we visualize Phoenix’s hidden state on a synthetic NIAH task:</p> <div style="margin: 1.5rem auto; display: flex; flex-direction: column; gap: 0.4rem; align-items: center; max-width: 100%;"> <div style="width: 100%; display: flex; justify-content: center; overflow: hidden;"> <iframe id="phoenix-memory-dynamics" src="/SSM_Story/assets/html/phoenix_memory_dynamics.html" title="Phoenix Memory Dynamics — interactive slot allocation visualization" loading="lazy" scrolling="no" style="display: block; width: 100%; height: 700px; border: 0; background: transparent;" onload="(function(f){try{const d=f.contentWindow.document;const r=function(){f.style.height='0px';const h=Math.max(d.body?d.body.scrollHeight:0,d.documentElement?d.documentElement.scrollHeight:0);f.style.height=(h+4)+'px';};r();setTimeout(r,150);setTimeout(r,600);}catch(e){}})(this)"></iframe> </div> <div style="width: 100%; font-size: 0.85rem; color: #475569; line-height: 1.8; text-align: left;"> <strong style="color: #1e293b; display: block; margin-bottom: 0.4rem;">Figure: Phoenix Memory Dynamics &mdash; Layer 23, Head 4.</strong> The box is Phoenix's recurrent memory: 512 slots arranged left-to-right, each displayed as a vertical line. The memory starts completely empty (gray) and fills token by token as the prompt is processed. <span style="color:#dc1010; font-weight:600;">&#9646; Red slots</span> are claimed by the passkey tokens (<em>RecallSSM1378</em>) — the router directs them into a dedicated region and keeps them there. <span style="color:#1a8fe0; font-weight:600;">&#9646; Blue slots</span> receive both passkey and context writes and act as shared memory. <span style="color:#12b554; font-weight:600;">&#9646; Green slots</span> absorb the surrounding context tokens only. Brightness grows each time a slot is revisited — darker means written once, fully saturated means written many times. When <strong>Phoenix:</strong> begins generating the answer, it reads from the red region and the retrieval slots brighten further, showing the model recovering exactly what it stored. </div> </div> <p>The passkey — shown in $\color{red}{red}$ — is routed into a dedicated region of the hidden state and remains separated from most ordinary tokens. Ordinary tokens shown in $\color{green}{green}$ flow through the general-purpose slots without touching it. Some slots shown in $\color{blue}{blue}$ serve both roles. Crucially, different heads allocate different amounts of memory to retrieval-critical tokens — the model discovered this specialization entirely on its own, without any explicit supervision.</p> <p>This is what <strong>“organizing memory like a closet”</strong> looks like from the inside.</p> <h2 id="final-notes-and-future">Final Notes and Future</h2> <p>Phoenix began from a simple goal: improving recall in linear models. More broadly, it suggests that learned memory can benefit from structured, content-based allocation rather than uniform updates across slots. In Phoenix, a relatively simple routing mechanism is enough to improve recall while remaining competitive on language modeling, with the additional benefit of strong length generalization.</p> <p>There is plenty left to explore. How far can the length generalization be pushed? Can the routing mechanism be made even more expressive? What happens at truly large scales? We don’t have all the answers yet — but we’re working on it.</p> <script>
  (() => {
    const iframe = document.getElementById('phoenix-recurrent-matrix-update');
    if (!iframe) return;

    const resizeIframe = () => {
      try {
        const doc = iframe.contentDocument || iframe.contentWindow?.document;
        if (!doc || !doc.body || !doc.documentElement) return;

        const height = Math.max(
          doc.body.scrollHeight,
          doc.documentElement.scrollHeight,
          doc.body.offsetHeight,
          doc.documentElement.offsetHeight
        );

        iframe.style.height = `${height}px`;
      } catch (error) {
        // Ignore cross-document timing issues during initial load.
      }
    };

    iframe.addEventListener('load', () => {
      resizeIframe();
      window.setTimeout(resizeIframe, 0);
      window.setTimeout(resizeIframe, 250);
      window.setTimeout(resizeIframe, 1000);
    });

    window.addEventListener('resize', resizeIframe);
  })();
</script>]]></content><author><name>Arshia Afzal$^*$ (Bloggin 🎙️)</name></author><summary type="html"><![CDATA[Architecture and Results]]></summary></entry><entry><title type="html">Story of Linear Time Sequence Modeling 📚</title><link href="https://arshiaafzal.github.io/SSM_Story/2025/lion-part3-chunk/" rel="alternate" type="text/html" title="Story of Linear Time Sequence Modeling 📚"/><published>2025-08-29T00:00:00+00:00</published><updated>2025-08-29T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/SSM_Story/2025/lion-part3-chunk</id><content type="html" xml:base="https://arshiaafzal.github.io/SSM_Story/2025/lion-part3-chunk/"><![CDATA[<hr/> <h1 id="why-linear-transformers">Why Linear Transformers?</h1> <p>If you’ve heard about Large Language Models (LLMs) and Transformers and are curious to learn more, there are already plenty of excellent blog posts, articles, and YouTube videos that explain them in great detail with amazing visualizations. In case your main interest is understanding LLMs and how Transformers work, we’d point you there first, they’ve done a fantastic job (honestly, better than us).</p> <p>In this series, we’ll cover Linear Transformers and State Space Models (SSMs), giving a high-level summary of their core ideas. So, if you’ve come across names like <strong>Mamba</strong> or <strong>DeltaNet</strong> and wondered what they are, or if you’ve asked yourself:</p> <p><strong>1)</strong> Why we moved from RNNs to Transformers?</p> <p><strong>2)</strong> Why we now seem to be circling back from Transformers toward Linear Transformers (almost like “RNNs on steroids”)?</p> <p>then this post should be a good fit 😉.</p> <p>So let’s start from answering the above questions</p> <hr/>]]></content><author><name>Arshia</name></author><summary type="html"><![CDATA[Summary of Linear Transformers]]></summary></entry><entry><title type="html">Story of Linear Time Sequence Modeling 📚</title><link href="https://arshiaafzal.github.io/SSM_Story/2025/lion-part4-results/" rel="alternate" type="text/html" title="Story of Linear Time Sequence Modeling 📚"/><published>2025-08-29T00:00:00+00:00</published><updated>2025-08-29T00:00:00+00:00</updated><id>https://arshiaafzal.github.io/SSM_Story/2025/lion-part4-results</id><content type="html" xml:base="https://arshiaafzal.github.io/SSM_Story/2025/lion-part4-results/"><![CDATA[<hr/> <h1 id="why-linear-transformers">Why Linear Transformers?</h1> <p>If you’ve heard about Large Language Models (LLMs) and Transformers and are curious to learn more, there are already plenty of excellent blog posts, articles, and YouTube videos that explain them in great detail with amazing visualizations. In case your main interest is understanding LLMs and how Transformers work, we’d point you there first, they’ve done a fantastic job (honestly, better than us).</p> <p>In this series, we’ll cover Linear Transformers and State Space Models (SSMs), giving a high-level summary of their core ideas. So, if you’ve come across names like <strong>Mamba</strong> or <strong>DeltaNet</strong> and wondered what they are, or if you’ve asked yourself:</p> <p><strong>1)</strong> Why we moved from RNNs to Transformers?</p> <p><strong>2)</strong> Why we now seem to be circling back from Transformers toward Linear Transformers (almost like “RNNs on steroids”)?</p> <p>then this post should be a good fit 😉.</p> <p>So let’s start from answering the above questions</p> <hr/>]]></content><author><name>Arshia</name></author><summary type="html"><![CDATA[Summary of Linear Transformers]]></summary></entry></feed>